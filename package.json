{
  "name": "@ev-core/main",
  "version": "0.6.2",
  "description": "Multiple libraries",
  "scripts": {
    "build": "pnpm -r --sort build",
    "clean": "pnpm -r run clean",
    "rebuild": "pnpm run clean && pnpm run build",
    "dev": "pnpm -r run dev",
    "release": "node scripts/release.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sondr/ev-core.git"
  },
  "author": "",
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "rimraf": "^6.0.1",
    "ts2mjs": "^4.0.0",
    "typescript": "^5.9.2"
  }
}